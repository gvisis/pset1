#include <stdio.h>
#include <cs50.h>
#include <math.h>

int main(void)
{
    do
    {
        long long ccnumber = get_long("Enter the credit card number: ");
    }
    while (ccnumber>)
    printf("Number is: %lli\n", ccnumber);
}
